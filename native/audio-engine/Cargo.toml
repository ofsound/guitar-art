[package]
name = "audio-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
cpal = "0.15"
napi = { version = "3", default-features = false, features = ["napi8"] }
napi-derive = "3"
rtrb = "0.3"
rustfft = "6"

[build-dependencies]
napi-build = "2"
